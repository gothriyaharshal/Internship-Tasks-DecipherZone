I/O >>

They are of many types 

>
console io ....file io .....network io ....Memory io

>
console io (Scanner or BufferReader)


 System.out.print();
 
Means System class having static out varible and out has print method

>>System. class imported in lang pacakge

class System
 {
   static PrintStream out ;
 }

//this out is an refrence of other class which providing us print method 

class PrintStream
{
   void print() {};
}


>>>>>

System.err.println("Invalid age");

>>>

Java i/o based on Stream by which it reads data from 

memory , file , console , Network

All of they are flow in byte formate 

>>>>

Obejct class Equal methods

bydefault it compares refrence of object if they are not same then it return false like

Student s1 = new Student();
Student s2 = new Student();

s1.equals(s2);   //false

but if

String s1 = new String("Hello");
String s2 = new String("Hello");
 
in String class there has equals method overridden and form like as it compares charactes

Integer , string , char type class iverrides equals method and give there own implementation for 
Student class we have also provide implementation so it check data then give us update 

>>Like in student class We wont to compare on the basis of id like two object id are equal or not

class Student {

    int id;

    Student(int id) {
        this.id = id;
    }

    public boolean equals(Object obj) {
        Student s = (Student)obj;
        return this.id == s.id;
    }
}

then if i do .....s1.equal(s2) ....on the basis of id it check if both are same it return true either return false


s1.equal(s2)   

here s1 is this....part .....s2 is obj

>>
like this.....equals(Object obj)  in equal parameter obj were going 

>>
We converts this obj into Student type 

Student s = (Student)obj ;

return this.id == s.id ;

>>if this.id == s.id then return true  

 means my s1 and s2 refrence are same then they are equal na  

>> if obj instanceof Student this condition is false then return false because
if s2 is not part of Student then it not means to comparing those

public boolean equals(Object obj) {

    if(this == obj)                  //this mean both oject refering to same location
        return true;

    if(!(obj instanceof Student))
        return false;

    Student s = (Student)obj;

    return this.id == s.id;
}

also remember this also

>>All wrapper classes is pre overridden an equal methods

>>Check out in A_Equal_Programm.java



import java.util.Scanner;
import java.util.*;


public class A_HashCode_Programm
{

int id ;

String name ; 


public int hashCode()
{
return this.id;
}


public boolean equals(Object obj)
{
   if(this == obj)
{
  return true;
}

 if(!(obj instanceof A_HashCode_Programm
))
{
return false;
}

A_HashCode_Programm a = (A_HashCode_Programm)obj;

if((this.id == a.id) && (this.name == a.name))
{
return true;
}

else
{
return false;
}

}


A_HashCode_Programm
(int id , String name)
{
  this.id = id ;
  this.name = name;
}


public static void main(String[] args)
{

A_HashCode_Programm a1 = new A_HashCode_Programm(1,"harshal");
A_HashCode_Programm a2 = new A_HashCode_Programm(1,"harshal");


HashSet<A_HashCode_Programm>  s = new HashSet<>();

s.add(a1);
s.add(a2);


System.out.println(s.size());

}
}


>>>>>>>

public int hascode()
{
 int result = 17;
 result = result *31 +age;
 result = result *31 +name.hashcode();   //name is string and string override hashcode

 return result;
}


what happens here like if some one age and name is same then there result means hashcode also same 

//null pointer exception na aaye islie 

(name == null ) ? 0 : name.hashcode() );



>>>>
nya trika

public int hashcode()
{
  return Objects.hash(name,age);
}



>>>>>>getClass() ....in object class

Student s1 = new Student();

s1.getClass();

>>>>>>>>clone()   (copy of an object)

koi bhi class clone ko thbi override kr pati h jb vo clonable ko implement krke rkhi hui ho

 >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>><<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

Enumaration

why this come

>>Suppose payment status is in three state --->> success , failed , pending

>>How did i represent those constent value in java (by final keyword)

public static final int success = 1;
public static final int failed = 2;
public static final int pending = 3;


>call them all by classname...
>
>>>>Problems which come from this

1> type safety ()

class PaymentStatus
{

public static final int success = 1;
public static final int failed = 2;
public static final int pending = 3;

}

class main
{
 psvm()
 {
  PaymentStatus p = new PaymentStatus();
  
  int status = p.success;
  
  int status = 100;

  }
}


https://github.com/adityatandon15/Java-Full-Course/blob/main/Lecture%2023/notes.pdf  

>>go and read notes


Interfac>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>.

all varible in interface are 
public statib and final  (static becuse interface has no relation with object and if there has no relation with object we also not pass varivle value in constructor also)

>>We lso override default method in interface

>>we also make an static method in interface

>>java 9 

i also create private method in java ....

private void run()
{
  sout("hy");
}

ab isse bs us interface k andr hi call kr skte h bahar access nhi kr skte 

>>Diamond problem resolve by interface



Class A (fun method)

class B extend A      and      class C extend A  

both gives there definition

class D extend A , B
{
   D d = new D();

  d.fun();          //here confusion creates
}


which fun() were called


>>interface solve it by 

A , B , C all are interface and has fun method declation of those were given in 

child class


>>>but in default method there has body then what we do here  


1 option>>

interface  a has fun default method

interface b has fun default method

class c implements a,b   

first option is override fun method give your own implementtatio

2 option>>

if i want that a ya b m se kisi ek ka chle 

toh m override toh kruga hi uske andr likhna hoga 


interface_name.super.function_name();


@Override
public void fun()
{
  B.super.fun();
} 



>>>java Resolution Rule

if any class Implement interface and extend class and those 2 have same name of function .
...and when i create object then java give priority to class function

interface A
{
  default void()
  {
   sout("inside interfave");
  }
}

class B
{
 void fun()
 {
   sout();
 }
}

class D extend B implements A
{
   
}

class main {
psvm()
{
 D n = new D();
 n.fun();        //always class one calls

}
}


//or if i wwant my own implementation then override that method in child class

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>><<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<


Strings

seq of char  internally char[] ch = [h , e,l,l,o]

but if we have char array then why we use String because ther has more functionality provided by it like comparision and etc.
  in char[] muje algorithm likhna pdta for spliting reversing sorting and etc and string provide them by defaultly


>>copile time data store in scp and runtime data store in heap

String s1 = "java" ;    it store in scp

String s2 = s1+"World"  ;   at runtime we took analyze what is in s1 so it store in heap


and "world" goes into scp  becuse every literals goes into scp 


>>
String s1 = "java";
String s2 = s1;

assignment wali chize compile time pr hi ho jati h dono scp m 


>>
String s1 = "java" ;    //it stores in scp

String s2 = new String("java");  //first this is litera; then java store it into scp 

then this new keyword force it to store into heap memory....java is already ther in scp 


>>
String s = new String("Hello");

first it goes into scp because it is litral then new force it to heap memory

Sring method in 26 lecture


>>AbstractStringBuilde child class are StringBuilder and StringBuffer



>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>><<<<<<<<<<<<<<<<<<<<<<<<<<<<<

Generic

in class Box if private int a ; ; .....and passing its value from constructor then i am only able to pass normal int value here
not other type

in java if i want to strore all type of data so i took type as Object

private Object a ;

provife getter and setter 

and when i ...call function like

Box b1 = new Box(1);   //here i pass any type of data 


>>>but here main problem is i didn't apply any operation on those 

like if id do..........  sout(b1.getValue() + 5 ) ; ............. then here i face error

>>here jvm gets confused that which type of data you were adding in
 which so here we have to do downcasting(Explicit type)


 
>>If i known in my data i have Integer then i type cast it into Integer

 Interger i = (Integer)b1.getValue();

>>>>>So here i face problem while making an Object Class so overcome this problem we have
generics

>>

  We are using ....Private Object a ;.......insted of it we use it like

 private T a ; ..............here tree represents any type of data...and we can took
                             any type of name here insted of T  and ...also apply 
                          syntax of generic class in front of class as like  
                                        class Box<T> ;



when i create its object then i write it as

---->>    Box<Integer> b1 = new Box<Integer>(10);


---->>    Box<String> b2 = new Box<String>("10");

i able to pass Any type of data and perform opration on them also

//yaha m aisa operation nhi perform kr skta jo runtime pr aaaye muje compile time pr hi pta chl jayega
kya error h like

if i do ......sout(b1.getValue() + 5);.........then it is ok but if i do it didn't identifies exact object it give us value of b1.getValue
 Also Store int value in it but when i do operation on that it confuses because it it object type and jvm not accept that you do any type of operation on any type

sou(b1.getValue() + substring(9));   //here compiler show error


______________>>>>

public class Dem0
{
 psvm()
 { 
  Pair<String> p1 = new Pair<>("Name" , "Aditya");

  Pair<Integer> p1 = new Pair<>(1 , 123);

 }
}

class Pair<T>
{
  T first ;
  T second ;
  
  Pair(T first , T second)
  {
   this.first = first;
   this.second = second;
  }
}


//here i disclose at creation of object that which type of data i want ....

___________>>>>

if i want that first parameter is different and second parameter is also different 

like first is of Integer type and Second is of double type then i pass at class levele



public class Dem0
{
 psvm()
 { 
  Pair<String , Interger> p1 = new Pair<>("Name" , 1);

  Pair<Integer , String> p1 = new Pair<>(1 , "123");

 }
}

class Pair<T , U>
{
  T first ;
  U second ;
  
  Pair(T first , U second)
  {
   this.first = first;
   this.second = second;
  }
}

_________>>>>>>>

if i want to make my method also of generic type like first it has 

public static int getResult( int x)
{
   return x+5;
}


but i want i select it at runtime .....i also provide here Object type but same problem occurs by object that ...if i do any String operation Type cast first it then i face problem
like first i typecast it into String and then perfrom int function


so here we were using generic


    public static  <T>  T printing(T a)    // <T>  here T is general declaration return 
                                              typr is T
    {
        return a;
    }
 

EXAMPLE

package Access_Modifier2;

class Parent<T>
{
    private T a ;
    private T b;
    public T getValue()
    {
        return a;
    }
    public T getB()
    {
        return b;
    }
    Parent(T a , T b)
    {
        this.a = a;
        this.b =b;
    }

    public static  <V>  V printing(V a)
    {
        return a;
    }

}
public class Just_Testing_3 {


    public static void main(String[] args) {
        Parent<Integer> p = new Parent<>(10,20);

        System.out.println(p.getValue() +5 );

        Parent<String> s = new Parent<>("Harshal","Gothriya");

        System.out.println(s.getValue().length() );

        Integer a = Parent.printing(12);
        System.out.println(a);

        String b = Parent.printing("ASADA");
        System.out.println(b);
    }
}


__________________>>>>>

class Box<T>
{ 
  T value ;    
}

i know that when i create object of that class then i pass any type of data here but i want to apply
  some type of restriction our there only Number type of data Stores like int ,double,float that type of data

class Box<T extends Number>
{
T value;
 }

//now only Number class extends here only int float, double , long etc number type store here



i want i also implement some interface here 

class Box<T extends Animal>   //this class work only with Animal or its sub class
{

}


___>
class Box<T extends Animal & swin>   //this class work only with Animal or its sub class
{                                   // and with interface swin
                                    //here has and operator so Animal and Swin both were       there
}

____>

<T extends class & Interface1 , Interface2>


____________________________________________________________________________________________________________________________________________________ ________________________________

Wilcard in Generics



Generic break an rule of parent child relationship

>> Animal a  = new Dog() ;
 
 it is possible in java

>> List<Dog> dogs = new ArrayList<>(); 
    
  is is also possible in java

>>
   List<Animal> animals =  new Dog();
   
it is not possible in java

i say in java that ....Dog is a Animal ....but i didn't say that list of dog is a List 

of Animal in java



Why this is not possible >>

List<Dog> dogs = new Arraylist<>();

List<Animal> li = dog;

when i do ....

li.add(new Animal()); ....then there in dog one more list were add of dog

>> Animal has two method ..eat() and...walk()  and dog has ....bark()

[d,d,d,d,A]  

>>now here animal one come at last if i calls

for(Dog d : li)
{
  d.bark();
}


then at last A there has no bark method ...so here type safty break this is known as

invariant in java


>>>in java if i have two array 
dog and animal 

Dog[] dogs = new Dog[10];

Animal[] animal = dogs;

animal[3] = new Animal();

then i face runtime error here


dogs and animal pointing two similar dogs array if i d0.....this then i am adding new Animal at dogs array then java so allow this

>>>>

void printAnimal (List<Animal> a)
{

  for(Animal n : a)
 {
  sout(n);
}
}

List<Dog> dog = new List<>();

printAnimal(dog)

this is not possible in java generic

>>>>

void printAnimal (Animal a)
{

  for(Animal n : a)
 {
  sout(n);
}
}

List<Dog> dog = new List<>();

printAnimal(dog)


but this is allowed

>>>>>>>>>>>>>>>>>>>>>>>>>>

for overcome this problem there has wildcard in javq

List<?> list = new ArrayLis<Integer / String / boolean> ();

any type of data i able to pass here 



>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>><<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

in stack collection there has add and remove method

add(6);   //at top

remove();  //from top

remove,entry[1,2,3,4]

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

Queue fifo 


remove[1,2,3,4]entry


remove() ; // 1 top element removes

entry(); // entery from last

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>><<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

Hashmamp

>By hashcode first we calculating value and by equals cheking it is equal or not

_________________________________________________________________________________________
Itreabale---->>Collection>>>>>List>>>>Set>>>>Queue

>>>
   List InterFace

* Elements have a value
* Duplicates are allowed
* Insertion order is preserved
* You can access Element by index


in java 21 >>> there one more interface were come in between Collection and list


SequecedCollection

>>Now our herarichy becomes

Collection(I)----->SequencedCollection(Interface)---->List(I)   we also using Sequeced collection method

Those all method are of List interface

>>getFirst()
>>getLast()
>>addFirst()          provided by sequencedCollection interface
>>addLast()
>>removeFirst()
>>removeLast()

>>get(index)
>>set(index,E value)     //set means updarting if there has pre value there
>>add(index , E value)   //by add we only adding new element and increasing size of Lists
>>addAll(index,collection<>)
>>indexof(object o)
>>listIterator(iteration forword and backword)
>>lastIndexof(Object O)
>>of () (by it we make our collection as immutable no changes there)
>>copyof() (copy in new and immutable)
remove(index)


>>For linked list java internally using doubly linked list


>>queue / stacke were implemetted by linkedlist

>>Arraydeque is....implements queue and stack


































0